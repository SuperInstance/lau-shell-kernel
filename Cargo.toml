[package]
name = "lau-shell-kernel"
version = "0.1.0"
edition = "2021"
description = "The bare shell kernel — the hermes-construct in Rust form"
license = "MIT"
repository = "https://github.com/SuperInstance/lau-shell-kernel"

[dev-dependencies]
tempfile = "3"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
